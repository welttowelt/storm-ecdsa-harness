---
name: paper-square-active-volume
description: "Storm repo-local skill for applying SQUARE active-volume ranking to lower-Q ecdsa.fail reuse and uncompute routes."
license: MIT
---

# Paper - SQUARE Active Volume

This is a Codex-discoverable bridge to the Storm prompt card at
`../../../skills/paper-square-active-volume.md`.

Before editing a lower-Q reuse, uncompute, or codec-timing route, read that card
and run the active-volume ledger on the current trace. Rank by rows that exceed
the target q tier, then combine the result with the trusted validation gates.

This bridge is local-only. It does not load private chat logs, private
endpoints, raw nonces, telemetry, or always-on behavior.
